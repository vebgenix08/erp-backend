import { requireAuth, requirePermission } from "@school-erp/auth";import { requireTenantId } from "@school-erp/tenancy";import type { RequestContext } from "@school-erp/api";import { numberingPermissions } from "./numbering.permissions";import { toNumberingPolicyView } from "./numbering.mapper";import { ensureDefaultNumberingPolicies, numberingRepository, type NumberingRepository } from "./numbering.repository";import { validateNumberingPolicy } from "./numbering.validator";import type { NumberingServiceContext } from "./numbering.model";
type Context=NumberingServiceContext|RequestContext;function tenant(c:Context){requireAuth(c.authContext);return requireTenantId(c.tenantContext);}export async function listNumberingPolicies(c:Context,deps:{repository?:NumberingRepository}={}){requirePermission(c.authContext,numberingPermissions.read);return(await ensureDefaultNumberingPolicies(deps.repository??numberingRepository,tenant(c))).map(toNumberingPolicyView);}export async function saveNumberingPolicy(input:unknown,c:Context,deps:{repository?:NumberingRepository}={}){requirePermission(c.authContext,numberingPermissions.manage);return toNumberingPolicyView(await(deps.repository??numberingRepository).upsert(tenant(c),validateNumberingPolicy(input)));}
