import test from "node:test";import assert from "node:assert/strict";import { InMemoryNumberingRepository,ensureDefaultNumberingPolicies } from "../numbering.repository";test("creates one default policy per numbering stream without duplicates",async()=>{const repo=new InMemoryNumberingRepository();const first=await ensureDefaultNumberingPolicies(repo,"tenant_1");const second=await ensureDefaultNumberingPolicies(repo,"tenant_1");assert.equal(first.length,14);assert.equal(second.length,14);assert.equal(new Set(second.map(v=>v.stream)).size,14);assert.equal(second.find(v=>v.stream==="STUDY_CERTIFICATE")?.format,"STU/{YEAR}/{SEQUENCE}");});test("isolates policies by tenant",async()=>{const repo=new InMemoryNumberingRepository();await ensureDefaultNumberingPolicies(repo,"tenant_1");assert.equal((await repo.list("tenant_2")).length,0);});
