import { BadRequestError } from "@school-erp/errors";import { PERMISSION_CATALOG,type AccessScope,type AccessScopeType } from "./access.model";const scopeTypes:AccessScopeType[]=["TENANT","CAMPUS","PROGRAM","CLASS","SECTION","ASSIGNED_ONLY"];const strings=(v:unknown)=>Array.isArray(v)?[...new Set(v.map(String).map(x=>x.trim()).filter(Boolean))]:undefined;export function validateScope(v:unknown):AccessScope{if(!v||typeof v!=="object"||Array.isArray(v))throw new BadRequestError("scope is required");const x=v as Record<string,unknown>;const scopeType=String(x.scopeType??"").toUpperCase() as AccessScopeType;if(!scopeTypes.includes(scopeType))throw new BadRequestError("scope type is invalid");const campusIds=strings(x.campusIds),programIds=strings(x.programIds),classIds=strings(x.classIds),sectionIds=strings(x.sectionIds);const result:AccessScope={scopeType,...(campusIds?{campusIds}:{}),...(programIds?{programIds}:{}),...(classIds?{classIds}:{}),...(sectionIds?{sectionIds}:{})};if(scopeType==="CAMPUS"&&!campusIds?.length)throw new BadRequestError("campus scope requires campusIds");if(scopeType==="PROGRAM"&&!programIds?.length)throw new BadRequestError("program scope requires programIds");if(scopeType==="CLASS"&&!classIds?.length)throw new BadRequestError("class scope requires classIds");if(scopeType==="SECTION"&&!sectionIds?.length)throw new BadRequestError("section scope requires sectionIds");return result;}export function validatePermissions(v:unknown){if(!Array.isArray(v))throw new BadRequestError("permissions are required");const allowed=new Set(PERMISSION_CATALOG.map(p=>p.code));const result=[...new Set(v.map(String))];if(result.some(p=>!allowed.has(p)))throw new BadRequestError("permission is not in the platform catalog");return result;}
