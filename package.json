{
  "name": "@school-erp/backend",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter \"./apps/*\" --filter \"./workers/*\" --if-present dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "turbo": "^2.3.3"
  }
}
